'use strict';

const modules = "./modules/";
const components = "./components/";

// LIBS

const anime = require('../../node_modules/animejs/lib/anime');

// MODULES / COMPONENTS

const _events = require(`${modules}events`);
const __text = require(`${components}text`);
const __text = require(`${components}landing-animations`);

document.addEventListener("DOMContentLoaded", function () {

    // anime({
    //     targets: 'div',
    //     translateX: 250,
    //     rotate: '1turn',
    //     backgroundColor: '#FFF',
    //     duration: 800,
    //     loop: true
    //   });

});